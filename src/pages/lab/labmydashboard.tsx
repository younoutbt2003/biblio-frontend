import React from 'react';
import { Dashboard } from '../researcher/dashboard';

// Simple wrapper qui utilise le même composant
export const LabMyDashboard: React.FC = () => {
  return <Dashboard />;
};